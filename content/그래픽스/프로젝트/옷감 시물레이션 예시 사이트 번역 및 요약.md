# 옷감 시물레이션 예시 사이트 번역 및 요약

목적 : 해당 사이트의 정보를 정리 요약해서, 다이렉트 11로 만들기

# 데이터 set 만들기

정사각형 형태로 만든다.

```python
import numpy as np
import os

def generate(side_length, n_seg):
    # sample nodes uniformly on a square
    x = np.array([[0.0, 0.0]] * ((n_seg + 1) ** 2))
    step = side_length / n_seg
    for i in range(0, n_seg + 1):
        for j in range(0, n_seg + 1):
            x[i * (n_seg + 1) + j] = [-side_length / 2 + i * step, -side_length / 2 + j * step]

    # connect the nodes with edges
    e = []
    # 세로
    for i in range(0, n_seg):
        for j in range(0, n_seg + 1):
            e.append([i * (n_seg + 1) + j, (i + 1) * (n_seg + 1) + j])
    # 가로
    for i in range(0, n_seg + 1):
        for j in range(0, n_seg):
            e.append([i * (n_seg + 1) + j, i * (n_seg + 1) + j + 1])
    # 대각선
    for i in range(0, n_seg):
        for j in range(0, n_seg):
            e.append([i * (n_seg + 1) + j, (i + 1) * (n_seg + 1) + j + 1])
            e.append([(i + 1) * (n_seg + 1) + j, i * (n_seg + 1) + j + 1])

    return [x, e]
```

    x = np.array([[0.0, 0.0]] * ((n_seg + 1) ** 2))

→ 정사각형 형태를 이루는 particle들을 생성합니다.

n_seg  = 1 → 4개의 정사각형을 이루는 particle 생성

![Untitled](attachments/Untitled_17.png)

n_seg  = 2 → 9개의 정사각형을 이루는 particle 생성

![Untitled](attachments/Untitled%201.png)

n_seg  = 3 → 16개의 정사각형을 이루는 particle 생성

![Untitled](attachments/Untitled%202.png)

해당 particle의 개수만큼 array 초기화 한다.

step은 길이 / particle의 개수 의 값으로 정해진다.

최종적으로  x,e가 return한다.

### x

- 각 particle의 2차원 위치 벡터 데이터 저장
- array 형식이고, 1차원 배열 형태

### e

- 가로,세로,대각선 관계로 연결되어 있는 particle index key : value 형태로 저장되어 있다.
- array 형식이고, 1차원 배열 형태

```python
v = np.array([[0.0, 0.0]] * len(x))             # velocity
m = [rho * side_len * side_len / ((n_seg + 1) * (n_seg + 1))] * len(x)  # calculate node mass evenly
# rest length squared
l2 = []
for i in range(0, len(e)):
    diff = x[e[i][0]] - x[e[i][1]]
    l2.append(diff.dot(diff))
```

v : 속도

m : 각 particle의 무게 (나중에 왜 이렇게 무게를 계산하는지 확인하기)

l2 : 각 연결된 particle 쌍의 초기 길이 설정

그 뒤는 화면 구성 함수 …

```python
# 시뮬레이션을 진행하고 화면 새로 고침을 기다립니다
[x, v] = time_integrator.step_forward(x, e, v, m, l2, k, h, 1e-2)
    time_step += 1
    pygame.time.wait(int(h * 1000))
    square_mesh.write_to_file(time_step, x, n_seg)
```

time_integrator.step_forward

→ h 시간이 지난 후의 위치와 속도를 업데이트를 하는 것이다.

→ 해당 동작은 implicit Euler method 사용한다.

## 관성 값(힘) 구하는 방법

$E_I(x)= \frac{1}{2}∥x−x^{~n}∥^2 *M$

$∇E_I(x)=M*(x−x^{~n})$

$∇^2E_I(x)=M$

```python
import numpy as np

def val(x, x_tilde, m):
    sum = 0.0
    for i in range(0, len(x)):
        diff = x[i] - x_tilde[i]
        sum += 0.5 * m[i] * diff.dot(diff)
    return sum

def grad(x, x_tilde, m):
    g = np.array([[0.0, 0.0]] * len(x))
    for i in range(0, len(x)):
        g[i] = m[i] * (x[i] - x_tilde[i])
    return g

def hess(x, x_tilde, m):
    IJV = [[0] * (len(x) * 2), [0] * (len(x) * 2), np.array([0.0] * (len(x) * 2))]
    for i in range(0, len(x)):
        for d in range(0, 2):
            IJV[0][i * 2 + d] = i * 2 + d
            IJV[1][i * 2 + d] = i * 2 + d
            IJV[2][i * 2 + d] = m[i]
    return IJV
```

- `val()` : 관성(힘)의 값을 계산합니다.
- `grad()` : 관성(힘)의 기울기(미분값)을 계산합니다.
- `hess()` : 관성(힘)의 헤세 행렬을 만듭니다.

[헤세 행렬(Hessian Matrix)의 기하학적 의미 - 공돌이의 수학정리노트 (Angelo's Math Notes)](https://angeloyeo.github.io/2020/06/17/Hessian.html)

## 질량-스프링

$P_e​(x)=l^2\frac{1}{2}​k(\frac{∥x1​−x2​∥^2}{l^2}​−1)^2$

질량-스프링 식을 사용해서,

```python
E_last = IP_val(x, e, x_tilde, m, l2, k, h)
p = search_dir(x, e, x_tilde, m, l2, k, h)
```

논문 정리

[[]]pi.com/2076-3417/11/17/8255

해당 논문에 암시적 통합  계산 방법이 있기에 사용한다.

 $( \dot{N} = \frac{\partial^2 C}{\partial x\partial t} = \frac{1}{|x|} \left[ \frac{(x \cdot \dot{x})}{|x|^2} \right] )$

[[Cloth-simulator]]
