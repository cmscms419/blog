# 레스터화의 원리

# 벡터와 포인트

|  | **벡터** | **포인트** |
| --- | --- | --- |
| **성질** | 방향과 크기 | 위치만 |
- point +/- vector = point

→ 어떤 위치에서 방향과 힘을 주면, 새로운 위치를 구할 수 있다.

- point - point = vector

→ 출발선에서 목적지까지 이동한 방향과 그 방향의 거리(힘)을 알 수 있다.

### 직선

$P(a) = P_0 + ad$

### 선분

$P(a) = (1 - a)P_0 +aQ$

$(0 ≤ a ≤ 1)$

## 래스터화가 빠른 이유

### 역방향 광추적

원래는 역방향 광추적을 사용했다

하지만 복잡한 계산식을 실시간으로 구현하기 어렵습니다.

### 래스터화

간단하게 말하면, 투영할 vertex를 투영한 다음에, 투영한 vertex만 렌더링하는 것이다.

- 바운드 박스 활용해서 투영되는 vertex만 계산할 수 있어서, 빠르다.
- vertex 안에 있는 것 픽셀도 계산해서 알 수 있다.

→ 어떻게 도형 내부에 픽셀을 칠할 수 있는가??

답은 “**벡터의 외적**”을 사용한다.

![](attachments/ray-triangle-barycentric.png)
[https://www.scratchapixel.com/lessons/3d-basic-rendering/ray-tracing-rendering-a-triangle/barycentric-coordinates.html](https://www.scratchapixel.com/lessons/3d-basic-rendering/ray-tracing-rendering-a-triangle/barycentric-coordinates.html)

예) 벡터 AC 와 벡터 AP의 외적을 구한다면, 값이 나온다.

그 값이 < 0 이렇게 되면, 도형 내부가 아닌 외부에 있다고 판단한다.

이런 식으로 서로 다른 벡터 3개(삼각형) 외적을 구한다. 3개의 외적의 값이 0 보다 크면 점 P는 삼각형 내부에 있다고 판단할 수 있다.

### 2차원 변환

크게 3가지가 있다.

- 이동 : Transform
- 회전: Rotation
- 스케일: Sclae

    ![Untitled](attachments/Untitled%201.png)


이 중 회전이 중요하다.

왜냐하면, **원점을 중심으로 회전**하기 때문이다.

![[/6.png]]

이 원점을 중심으로 회전하기 때문에,

이동하고 회전 시키면, 원점을 중심으로 회전하기 때문에 이동한 후에 위치에서 회전하지 않고, 원점을 중심으로 전체적인 정점이 회전한다.

### Depth buffer (깊이 버퍼, z-buffer)

앞과 뒤에 있는 물체 중에 앞에 있는 물체가 그려져야 한다.

## 쉐이더의 개념

![Untitled](attachments/Untitled%202.png)

그리고 왜 이런 구조로 만들어지는 지 알 수 있다.

- 그래픽스에서는 GPU 안에서 돌릴 수 있는 프로그램이라고 한다.
- 원래는 쉐이딩을 할 수 있는 기능이었다.

텍스처 좌표는 UV 좌표 이다.

vertex shader의 역할: 정점을 옮겨주는 역할

pixel, fragment shader는 색을 입혀주는 역할을 한다.

## back-face culling

culling → 뺀다.

원근 투영

![Untitled](attachments/Untitled%203.png)

가상공간의 물체를 눈 방향으로 투영한다.

이 때, 교차하는 평면에 투영한다.

- $a_z$은 0이다,
    - 왜냐하면, 평면이라서, 값이 0이다.

A’의 좌표를 구하는 방법은

눈과 시점 사이의 거리를 p라고 부르면

## $\frac{a'_y}{p} = \frac{a_y}{p + a_z}$

$a_x$도 위와 같은 식으로 구할 수 있다.

## 왜곡이 생기는 이유

3차원 공간과 2차원 공간은 다르다.

![](attachments/side_view_frustum.png)
[8.3 - Perspective Projections — LearnWebGL (brown37.net)](http://learnwebgl.brown37.net/08_projections/projections_perspective.html)

- 요약
    - 카메라와 vertex(x, y, z)의 거리와 z(depth)는 다르다.
    - 뒤에서 깊이 버퍼에 저장된 z로부터 거리를 계산할 수 있다.
- 원근 투영에서는 아래 그림에서 한 점 (x, y, z)가 Near clipping plane 위의 (x', y', z')으로 투영됩니다.
- 삼차원 공간상의 (x, y, z)에 있는 물체에 의해서 (x', y', z') 위치에 있는 픽셀의 색이 결정되는 것입니다.
- 그러나 그 거리가 픽셀의 깊이(depth)와는 다릅니다.
- (x', y, z') 위치에 있는 픽셀의 **깊이** 값은 z - near입니다.
- 카메라와 (x, y, z)의 **거리**는 카메라의 좌표가 (0, 0, 0)이라면 sqrt(x*x + y*y + z*z) 입니다.
- 깊이로부터 거리를 계산할 수 있습니다. 카메라로부터 픽셀 (x', y', z')로 향하는 방향을 계산할 수 있기 때문입니다. 뒤에서 투영 행렬의 역행렬을 사용하게 됩니다.

→출처 홍정모 그래픽스 파트 2 원근 투영

### 블린-퐁 쉐이딩

지역

- 빠르다.

전역

- 표현할 수 있는 빛효과가 많다.

→ 지역 빛 효과에다가 덧붙이는 형식으로 만든다.

빛의 효과에는 크게 3가지가 있다.

**Ambient, Diffuse, Specular**

[[basic 조명]]

간단하게

- **Ambient : 주변광**
- **Diffuse : 분산광**
- **Specular : 정반사**

## 조명

### Directional Light

멀리서 일정한 방향으로 향하는 빛

태양같은 멀리 있는 광원

### Point Light

사방으로 나가는 광원

광원의 위치가 중요하다.

위치에서 빛이 발사되기 때문이다.

### Spotlight

일정 범위에서 비치는 광원입니다.

적은 연산으로 그럴듯하게 만들 수 있는가?

[[  Light casters  ]]

위 링크에 자세히 나옴
