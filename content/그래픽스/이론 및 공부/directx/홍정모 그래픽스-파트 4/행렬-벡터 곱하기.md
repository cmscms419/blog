# 행렬-벡터 곱하기

GPU에서 행렬 벡터를 사용할 때, texture를 사용한다.

![Untitled](attachments/Untitled_17.png)

행렬 계산을 할 수 있다.

왼쪽은 행렬

오른쪽은 벡터

- GPU에 행렬 * 벡터
    - 행렬을 곱할 때, 넘겨주는 값이 float이다.
    - 행렬에 맞는 값을 곱해주기 위해서, 행(가로)의 값을 4로 나누어서 벡터와 비슷하게 맞춘다.