# factory

# factory pattern 정의

부모 클래스에서 객체들을 생성할 수 있는 인터페이스를 제공하지만, 자식 클래스들이 생성될 객체들의 유형을 변경할 수 있도록 하는 생성 패턴입니다.

# 왜 만들었는 가???

A 클래스에 B 클래스가 연결되어 있는 상태이다. A 클래스에는 B 클래스에 대한 메소드와 매개변수들이 있다고 가정합니다.

이 상태에서 C 클래스를 추가하려면, A 클래스의 코드를 변경해야 합니다.

이 상태에서 D, F ,G 클래스를 추가한다면, 아주 많은 코드 변경이 일어나게 됩니다.